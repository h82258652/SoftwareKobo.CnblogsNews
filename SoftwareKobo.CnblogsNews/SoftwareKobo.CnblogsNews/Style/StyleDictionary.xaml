<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Style x:Key="PageStyle"
           TargetType="TextBlock">
        <Setter Property="FontSize"
                Value="{ThemeResource TextStyleLargeFontSize}" />
        <Setter Property="HorizontalAlignment"
                Value="Center" />
        <Setter Property="VerticalAlignment"
                Value="Center" />
    </Style>
    <Style x:Name="NewsItemTitleStyle"
           TargetType="TextBlock"
           BasedOn="{StaticResource ListViewItemTextBlockStyle}">
        <Setter Property="TextWrapping"
                Value="Wrap" />
    </Style>
    <Style x:Name="NewsItemPublishTimeStyle"
           TargetType="TextBlock"
           BasedOn="{StaticResource ListViewItemSubheaderTextBlockStyle}">
    </Style>
    <Style x:Name="NewsDetailTitleStyle"
           TargetType="TextBlock">
        <Setter Property="FontSize"
                Value="{ThemeResource TextStyleExtraExtraLargeFontSize}" />
        <Setter Property="TextWrapping"
                Value="Wrap" />
        <Setter Property="HorizontalAlignment"
                Value="Center" />
        <Setter Property="VerticalAlignment"
                Value="Center" />
        <Setter Property="Margin"
                Value="10,0,10,0" />
    </Style>
</ResourceDictionary>